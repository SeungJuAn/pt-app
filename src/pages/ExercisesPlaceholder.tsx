import { Stack, Text, Title } from '@mantine/core';

export function ExercisesPlaceholder() {
  return (
    <Stack>
      <Title order={2}>운동 관리</Title>
      <Text c="dimmed">곧 추가됩니다.</Text>
    </Stack>
  );
}
